#!/usr/bin/env python3
"""
metadb - Palimpsest database management command-line interface

Description:
    Database management CLI for Palimpsest metadata operations.
    Provides commands for database initialization, migrations, backups,
    health monitoring, analytics, and maintenance.

Common Commands:
    metadb init              # Initialize database and Alembic migrations
    metadb stats --verbose   # Show database statistics
    metadb health            # Run health check with recommendations
    metadb backup            # Create timestamped database backup
    metadb analyze           # Generate detailed analytics report
    metadb cleanup           # Remove orphaned records
    metadb optimize          # Optimize database (VACUUM + ANALYZE)

Migration Commands:
    metadb migration-create MESSAGE      # Create new migration
    metadb migration-upgrade [REV]       # Upgrade to revision
    metadb migration-status              # Show migration status

Export Commands:
    metadb export-csv OUTPUT_DIR         # Export to CSV files
    metadb export-json OUTPUT_FILE       # Export to JSON
    metadb show 2024-01-15 --full        # Display entry details

Query Commands:
    metadb years                         # List years with entry counts
    metadb months YEAR                   # List months in year
    metadb batches                       # Show export batch structure

Implementation:
    Thin wrapper around 'python -m dev.database.cli'
    Run 'metadb <command> --help' for detailed options

Installation:
    make install   # Creates symlink in ~/.local/bin/metadb

Author: Part of Palimpsest project
License: MIT
"""

from dev.database.cli import main

if __name__ == "__main__":
    main()
