{#
  Arc index page.

  All arcs sorted by entry count with date range and description.

  Context:
    arcs: list of dicts:
      - name: str
      - description: str or None
      - entry_count: int
      - first_date: str (ISO date) or None
      - last_date: str (ISO date) or None
#}
# Arcs

---
{% for arc in arcs %}

### {{ arc.name | wikilink }}
{{ arc.entry_count }} entr{{ "y" if arc.entry_count == 1 else "ies" }}{% if arc.first_date and arc.last_date %} · {{ arc.first_date | flexible_date }} – {{ arc.last_date | flexible_date }}{% endif %}
{% if arc.description %}
{{ arc.description }}
{% endif %}
{% endfor %}
