{# Cities Index Template - Groups by country/region #}
# Palimpsest — Cities

Index of all cities, organized by country and region.

{% for country in countries|sort %}
{% set regions = groups[country] %}

## {{ country }}

{% for region in regions|sort %}
{% set region_cities = regions[region] %}
{% if region != 'Unspecified' %}
### {{ region }}

{% endif %}
{% for city in region_cities|sort(attribute='mentions', reverse=true) %}
- [[{{ city.path }}|{{ city.name }}]] ({{ city.mentions }} mention{{ 's' if city.mentions != 1 else '' }})
{% endfor %}

{% endfor %}
{% endfor %}
---

## Statistics

- **Total Cities**: {{ total }}
- **Countries**: {{ countries|length }}
