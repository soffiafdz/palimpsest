{#
  Entry index page.

  Year-level summary with links to per-year detail pages.

  Context:
    total_count: int (total entries across all years)
    year_summaries: list of dicts:
      - year: int
      - count: int
      - first_date: str (YYYY-MM-DD)
      - last_date: str (YYYY-MM-DD)
#}
# Entries

**{{ total_count }}** entries total

---
{% for ys in year_summaries %}

### [[/indexes/entries-{{ ys.year }}|{{ ys.year }}]] · {{ ys.count }} entr{{ "y" if ys.count == 1 else "ies" }}

{{ ys.first_date }} – {{ ys.last_date }}
{% endfor %}
