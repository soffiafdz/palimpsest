{# People Index Template - Groups by relationship category #}
# Palimpsest — People

Index of all people mentioned in the journal, organized by relationship category.

{% for category in categories %}
{% set category_people = groups[category] %}
{% if category_people %}

## {{ category }}

{% for person in category_people|sort(attribute='mentions', reverse=true) %}
- [[{{ person.path }}|{{ person.name }}]] ({{ person.mentions }} mention{{ 's' if person.mentions != 1 else '' }})
{% endfor %}
{% endif %}
{% endfor %}

---

## Statistics

- **Total People**: {{ total }}
- **Categories**: {{ categories|select('in', groups)|list|length }}
