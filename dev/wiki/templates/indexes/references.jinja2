{#
  Reference index page.

  Sources grouped by type (book, article, film, song, etc.)
  with author and reference count. Sorted by frequency within
  each group.

  Context:
    groups: dict of str (type) -> list of dicts:
      - title: str
      - author: str or None
      - reference_count: int
#}
# References

---
{% for type_name, sources in groups.items() %}

## {{ type_name | capitalize }}
{% for source in sources %}
- {{ source.title | wikilink }}{% if source.author %} by {{ source.author }}{% endif %} · {{ source.reference_count }} references
{% endfor %}
{% endfor %}
