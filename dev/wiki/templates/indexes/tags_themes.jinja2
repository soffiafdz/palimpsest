{#
  Tags & Themes index page.

  Frequency-sorted lists of tags and themes with usage counts.
  Only items with 2+ uses are included (filtered by context builder).

  Context:
    tags: list of dicts:
      - name: str
      - count: int
    themes: list of dicts:
      - name: str
      - count: int
#}
# Tags & Themes

---
{% if tags %}

## Tags
{% for tag in tags %}
- {{ tag.name | wikilink }} ({{ tag.count }})
{% endfor %}
{% endif %}
{% if themes %}

## Themes
{% for theme in themes %}
- {{ theme.name | wikilink }} ({{ theme.count }})
{% endfor %}
{% endif %}
