# {{ name }} — Entries

{{ entry_count }} entr{{ "y" if entry_count == 1 else "ies" }}

---
{% for y in year_summary %}
- **{{ y.year }}** — [{{ y.count }} entr{{ "y" if y.count == 1 else "ies" }}]({{ slug }}-entries-{{ y.year }})
{% endfor %}
