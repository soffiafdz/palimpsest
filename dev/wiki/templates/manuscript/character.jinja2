# {{ name }}
{% if role %}

**Role:** {{ role }}{{ " · **Narrator**" if is_narrator else "" }}
{% endif %}
{% if description %}

{{ description }}
{% endif %}

{{ scene_count }} scene{{ "s" if scene_count != 1 else "" }} · {{ chapter_count }} chapter{{ "s" if chapter_count != 1 else "" }}

---
{% if based_on %}

## Based On
{% for person in based_on %}
- *{{ person.contribution }}* · {{ person.person_name | wikilink }}
{% endfor %}
{% endif %}
{% if scenes %}

## Scenes
{% for s in scenes %}
- {{ s.name | wikilink }}{{ " (" ~ s.chapter ~ ")" if s.chapter else "" }}
{% endfor %}
{% endif %}

---

- [Edit metadata]({{ "character_yaml" | source_path(slug) }})
