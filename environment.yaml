name: palimpsest
channels:
  - conda-forge
dependencies:
  # Stable version of Python
  - python=3.10

  # Recent version of make
  - make=4.3

  # core runtime
  - ftfy
  - pyyaml
  - pip
  - pandoc=2.19
  - textstat

  # nvim
  - pynvim

  # pip installs
  - pip:
    - pypandoc
